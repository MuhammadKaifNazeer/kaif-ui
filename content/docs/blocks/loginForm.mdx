---
title: LoginForm
description: This is LoginForm 
---

import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import { Step, Steps } from "fumadocs-ui/components/steps";   
import {LoginForm} from "@/kaifui/blocks/LoginForm";

Looking for a simple yet powerful LoginForm   

## Preview

<Tabs items={["preview", "code"]}>
    <Tab value={"preview"}>    
       <LoginForm />  
    </Tab>
    <Tab value={"code"}>
```bash
import { Button } from "@/components/ui/button";
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

export const description =
  "A simple login form with email and password. The submit button says 'Sign in'.";

export function LoginForm() {
  return (
    <>
      <div className="w-full flex items-center justify-center h-screen bg-background rounded-xl">
        <Card className="max-w-sm mx-auto bg-background">
          <CardHeader>
            <CardTitle className="text-2xl">Login</CardTitle>
            <CardDescription>
              Enter your email below to login to your account.
            </CardDescription>
          </CardHeader>
          <CardContent className="grid gap-4">
            <div className="grid gap-2">
              <Label htmlFor="email">Email</Label>
              <Input
                id="email"
                type="email"
                placeholder="m@example.com"
                required
              />
            </div>
            <div className="grid gap-2">
              <Label htmlFor="password">Password</Label>
              <Input id="password" type="password" required />
            </div>
          </CardContent>
          <CardFooter>
            <Button className="w-full">Sign in</Button>
          </CardFooter>
        </Card>
      </div>
    </>
  );
}
```
    </Tab>
 </Tabs>

## Installation

<Steps>

<Step>

  ### Copy paste the code

```ts title="components/LoginForm.tsx" 
import { Button } from "@/components/ui/button";
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

export const description =
  "A simple login form with email and password. The submit button says 'Sign in'.";

export function LoginForm() {
  return (
    <>
      <div className="w-full flex items-center justify-center h-screen bg-background rounded-xl">
        <Card className="max-w-sm mx-auto bg-background">
          <CardHeader>
            <CardTitle className="text-2xl">Login</CardTitle>
            <CardDescription>
              Enter your email below to login to your account.
            </CardDescription>
          </CardHeader>
          <CardContent className="grid gap-4">
            <div className="grid gap-2">
              <Label htmlFor="email">Email</Label>
              <Input
                id="email"
                type="email"
                placeholder="m@example.com"
                required
              />
            </div>
            <div className="grid gap-2">
              <Label htmlFor="password">Password</Label>
              <Input id="password" type="password" required />
            </div>
          </CardContent>
          <CardFooter>
            <Button className="w-full">Sign in</Button>
          </CardFooter>
        </Card>
      </div>
    </>
  );
}
```
</Step>
<Step>
  ### Use in page

```ts title="app/page.tsx" 
import LoginForm from "@/components/LoginForm";
import Link from "next/link";

export default function LoginForm() {  
  return (
    <>
      <LoginForm />   
    </>
  );
}
```
</Step>
</Steps>

## Tech Stack

<Cards>
  <Card title="React" />
  <Card title="Next.js" />
  <Card title="Shadcn UI" />
  <Card title="Tailwind CSS" />
</Cards>
